const serverConfig = {
    "apiPrefix": "http://localhost:8080",
    "serverPort": "8080",
    "db": {
        "name": "edds-data-db",
        "host": "localhost",
        "port": 27017
    }
};

export default serverConfig;

