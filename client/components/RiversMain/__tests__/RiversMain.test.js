import React from 'react';
import { shallow, mount } from 'enzyme';
// import RiversMain from '../RiversMain.jsx';

describe('<RiversMain />', () => {
    it('should render RiversMain', () => {
        //     const renderedComponent = shallow(
        //         <RiversMain />
        //     );
        //     // // Выведем отрендеренный компонент
        //     // console.log(renderedComponent.debug());
        //     // Find FiresMain Page
        //     expect(renderedComponent.find('div .EddsData__RiversMain').hasClass('EddsData__RiversMain')).toBe(true);
        return undefined;
    });
});
