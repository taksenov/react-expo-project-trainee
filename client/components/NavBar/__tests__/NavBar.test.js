import React from 'react';
import { shallow } from 'enzyme';
import NavBar from '../NavBar.jsx';

describe('<NavBar />', () => {
    it('should render Navbar with menuElements', () => {
        // const renderedComponent = shallow(<NavBar />);
        // // // Выведем отрендеренный компонент
        // // console.log(renderedComponent.debug());
        // // Find NavBar on page
        // expect(
        //     renderedComponent.find('.navbar').hasClass('EddsData__NavBar')
        // ).toBe(true);
        // // Find menuElements of NavBar
        // expect(
        //     renderedComponent.find('Link .navbar-brand').children().node
        // ).toEqual('EDDS Data');
        // expect(
        //     renderedComponent.find('Link .NavBar__Notes_link').children().node
        // ).toEqual('Notes');
        // expect(
        //     renderedComponent.find('Link .NavBar__Rivers_link').children().node
        // ).toEqual('Реки');
        // expect(
        //     renderedComponent.find('Link .NavBar__Fires_link').children().node
        // ).toEqual('Fires');
        return undefined;
    });
});
